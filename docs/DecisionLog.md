# DecisionLog — 주요 의사결정 기록

> 형식: 무엇을, 왜 그렇게 결정했는지. 번복 시 새 항목으로 추가하고 이전 항목에 링크.

## D-001 (2026-07-14) 기반 기술: Claude API 직접 구현

- **결정**: Messages API + tool use 위에 에이전트 루프와 오케스트레이션을 직접 구현한다.
- **대안**: (a) Claude Agent SDK — Claude Code 하네스를 라이브러리로 사용,
  (b) Managed Agents — Anthropic이 루프·샌드박스 호스팅(베타, multiagent coordinator 내장).
- **근거**: 오케스트레이션 자체가 이 프로젝트의 핵심 산출물이므로 통제력이 최대인 층을 선택.
  메시지 패싱 구조(D-003)는 자체 구현이 필요해 기성 하네스의 이점이 줄어듦.
- **결정자**: 사용자.
- **수정**: [D-009](#d-009-2026-07-14-llm-계층-멀티-프로바이더-추상화)에서 일부 수정 —
  "직접 구현" 원칙은 유지하되, LLM 호출 계층은 Claude 전용이 아닌 멀티 프로바이더로 확장.

## D-002 (2026-07-14) 언어: Python

- **결정**: Python으로 구현한다.
- **근거**: 이전 프로젝트 환경이 Python(.venv + unittest)이었고, anthropic SDK와
  asyncio 기반 비동기 처리가 잘 갖춰져 있음.
- **결정자**: 사용자.

## D-003 (2026-07-14) 오케스트레이션 패턴: 자율 협업 (메시지 패싱)

- **결정**: 중앙 조율자가 태스크를 분배하는 방식이 아니라, 에이전트들이 메시지 버스를 통해
  서로 직접 메시지를 주고받으며 협업하는 구조로 한다.
- **대안**: 오케스트레이터-워커, 파이프라인(역할 체인).
- **근거**: 사용자 선택. 유연성이 가장 높은 패턴. 단, 수렴 제어가 어려우므로
  종료 정책(결과 제출 도구 + 예산 상한 + 유휴 감지)을 시스템 차원에서 강제한다(Plan M2).
- **결정자**: 사용자.

## D-004 (2026-07-14) 처리 대상: 범용 태스크

- **결정**: 특정 도메인(코딩/리서치 등)에 고정하지 않고, 팀 구성(역할 프롬프트)을
  설정 파일로 정의해 어떤 태스크든 처리할 수 있게 한다.
- **근거**: 사용자 선택. 팀 구성이 데이터(설정)로 분리되어 있으면 도메인 특화 팀은
  설정 추가만으로 파생 가능.
- **결정자**: 사용자.

## D-005 (2026-07-14) 인터페이스: 로컬 서버 + 웹 대시보드

- **결정**: FastAPI 기반 로컬 서버가 세션을 관리하고, 웹 대시보드에서 태스크 제출·
  메시지 흐름 관찰·결과 확인을 한다.
- **근거**: 사용자 선택. 메시지 패싱 구조는 흐름이 비선형이라 시각적 관측성이 중요.
- **결정자**: 사용자.

## D-006 (2026-07-14) 서버 스택: FastAPI + uvicorn + SSE

- **결정**: 서버는 FastAPI + uvicorn, 실시간 스트림은 SSE(Server-Sent Events)로 한다.
- **대안**: Flask(동기라 부적합), aiohttp(저수준), WebSocket(양방향 필요성 낮음).
- **근거**: asyncio 네이티브(요청 경로 블로킹 금지 제약과 부합), 자동 문서화,
  SSE는 단방향 이벤트 스트림에 충분하고 구현이 단순. 대시보드→서버 입력은 REST로 충분.
- **결정자**: Claude 제안, 사용자 이의 없으면 유지. (변경 원하면 이 항목 갱신)

## D-007 (2026-07-14) 기본 모델: claude-opus-4-8, 에이전트별 설정 가능 — D-008로 번복

- **결정**: 에이전트 기본 모델은 `claude-opus-4-8`로 하되, 팀 설정에서 에이전트별로
  모델을 지정할 수 있게 한다(경량 역할에 `claude-haiku-4-5` 등).
- **근거**: 범용 태스크의 품질 기준을 맞추려면 상위 모델이 기본값이어야 함.
  비용 절감은 사용자가 팀 설정에서 명시적으로 선택. adaptive thinking + effort 파라미터 사용.
- **결정자**: Claude 제안, 사용자 이의 없으면 유지.
- **번복**: 같은 날 [D-008](#d-008-2026-07-14-기본-모델-openai-gpt-56-terra-d-007-번복)로 번복됨.

## D-008 (2026-07-14) 기본 모델: OpenAI gpt-5.6 Terra (D-007 번복)

- **결정**: 에이전트 기본 모델을 OpenAI **GPT-5.6 Terra** 티어로 변경한다.
  에이전트별 모델 오버라이드(팀 설정)는 D-007과 동일하게 유지.
- **근거**: 사용자 결정. ChatGPT subscription 연동을 전제로 함.
- **사실 확인 (2026-07-14 웹 조사, Research.md §6)**:
  - GPT-5.6은 2026-07-09 출시된 3티어 패밀리 — Sol($5/$30), Terra($2.50/$15),
    Luna($1/$6, $/1M tokens). Terra는 GPT-5.5급 성능의 중간 티어. 티어 선택은 사용자(Terra).
  - ChatGPT 구독과 API 키 과금은 **별개 시스템**이므로 구독으로 일반 API 호출은 불가.
    단 "Sign in with ChatGPT"(OAuth, BYOS) 경로로 사용자 구독 quota 사용이 가능하다는
    조사 결과 — 자체 앱에서의 실현 가능성은 `확실하지 않음`, M2 착수 전 스파이크로 검증
    (실패 시 `OPENAI_API_KEY` 과금 폴백 후 이 항목 갱신).
  - ~~정확한 API 모델 ID(`gpt-5.6-terra` 추정)는 `확실하지 않음`~~ →
    **확정: `gpt-5.6-terra`** (2026-07-14 스파이크 — openai SDK 2.45.0 타입 정의).
- **결정자**: 사용자.
- **갱신(2026-07-14 스파이크)**: subscription 연동 전제는 "가능하지만 비공식"으로
  검증됨 — 인증 경로는 D-026(하이브리드)으로 확정.

## D-009 (2026-07-14) LLM 계층: 멀티 프로바이더 추상화

- **결정**: LLM 호출 계층을 프로바이더 중립 인터페이스(계약)로 추상화한다.
  계약은 M1에서 확정하고, **OpenAI 어댑터를 우선** 구현, Anthropic 어댑터는 후순위.
  팀 설정에서 에이전트별로 GPT/Claude 혼용 가능하게 한다.
- **대안**: OpenAI 단일(추상화 없음) — 단순하지만 Claude 혼용 시 재작업 필요.
- **근거**: 기본 모델이 GPT-5.6으로 바뀌며(D-008) D-001의 "Claude Messages API 직접 구현"
  전제가 깨짐. 오케스트레이션을 직접 소유한다는 D-001의 핵심은 유지하고 프로바이더만
  추상화. 아직 코드가 없어 전환 비용 0. "계약 우선" 개발 지침과도 부합.
- **결정자**: Claude 제안, 사용자 승인.

## D-010 (2026-07-14) 패키지/프로젝트 이름: hwabaek

- **결정**: 패키지·프로젝트 이름을 `hwabaek`으로 한다. (기존 작업명 `agora`는 PyPI 등록됨)
- **대안**: PyPI 미등록 확인된 후보 — thinktank, convene, warroom, moot, backchannel,
  sidebar, dure, watercooler, jamsession, salon, pumasi, madang, folkmoot, teamloop 등.
- **근거**: 화백(和白) — 신라의 만장일치 합의 회의체. 중앙 지시자 없이 구성원 간 논의로
  결정하는 구조가 이 시스템의 자율 협업(메시지 패싱, D-003) 패턴과 정확히 일치.
  고유명사라 검색·패키지 충돌 없음. PyPI 미등록 확인(2026-07-14).
- **결정자**: 사용자.

## D-011 (2026-07-14) 결과 확정: 화백 합의 모드 (투표 승인)

- **결정**: `submit_result`는 즉시 종료가 아니라 **합의 절차**를 연다. 제출된 초안이
  전원에게 브로드캐스트되고, 다른 에이전트들이 `vote_result`(approve/reject + 사유)로
  투표한다. 정족수는 팀 설정 `termination.approval`로 지정 — 기본 **unanimous**
  (제출자 제외 전원 승인), 옵션 `majority`(과반) / `first`(선착순, 투표 생략).
- **대안**: (a) 선착순 — 단순하지만 품질 검증 없음, (b) 선착순 + 합의는 추후 옵션.
- **근거**: 사용자 선택. 프로젝트 이름(화백 = 만장일치 회의체)의 정체성에 부합하고,
  결과 품질 검증(상호 비판)이 시스템에 내장됨. 순회 비용 증가와 교착 위험은
  기권 처리·no_quorum 실패·예산 상한으로 방어 (Plan "코어 의미론" §5).
- **결정자**: 사용자.
- **개정**: D-016에서 정족수 의미론 개정 — "기권 제외 전원 승인" 규칙은 엄밀한
  만장일치가 아니어서 `participating_unanimous`로 분리되고, 기본 `unanimous`는
  생존 심의자 전원 approve로 엄밀화됨.

## D-015 (2026-07-14) Hermes 미도입 — 화백 코어 직접 구현 유지

- **결정**: Hermes(외부 에이전트 런타임/워커 프레임워크) 기반으로 전환하지 않고
  기존 화백 코어(세션 상태 기계, asyncio 메시지 버스, 인박스 배치 소비, 종료 정책,
  제안·투표·반려·재합의, 도메인 이벤트, 결정 기록)를 직접 소유·구현한다 (D-001 재확인).
- **근거**: Hermes는 장기 실행 작업·작업 큐·프로필별 워커에 강점이 있으나, 화백의
  핵심인 실시간 세션 협업·메시지 배치 소비·합의 의미론과는 모델이 다름. 지금 전환하면
  의존성만 늘고 화백 전용 로직은 대부분 재구현 필요.
- **후속 조치**: 현재 마일스톤에서 도입하지 않음. 후순위 실험 대상으로만 기록 —
  향후 웹 조사/코드 수정/장기 실행 작업자가 필요할 때 외부 Agent Runtime 또는
  Worker Adapter로 검토. Hermes SDK나 전용 타입을 코어 계약에 노출하지 않는다.
  에이전트 실행부는 세션·합의 로직과 결합도를 낮게 유지하되(추후 AgentRuntime
  Protocol 추출 가능하게), 현재는 별도 Runtime 추상화를 만들지 않는다.
- **결정자**: 사용자.

## D-016 (2026-07-14) 합의 의미론 개정: 정족수 4종 + 제안 버전 + voting 잠금 (D-011 개정)

- **결정**:
  1. 정족수 정책을 4종으로 구분한다 — `unanimous`(기본): 제출자 제외 **생존 심의자
     전원** approve. `majority`: 생존 심의자 **전체의 과반** approve(유효 투표 과반
     아님). `participating_unanimous`: 유효 투표자 전원 approve(구 D-011 규칙이 이 이름
     으로 이동). `first`: 투표 없이 즉시 확정.
  2. **미투표는 암묵적 승인이 아니다** — unanimous에서 투표 시간 만료 시 미투표자가
     있으면 승인하지 않고 no_quorum 경로로 처리.
  3. **제안 버전**: `ResultProposal`(id/session_id/proposer/version/content/created_at)
     도입. 모든 투표는 proposal_id 필수, 이전 제안에 대한 늦은 투표는 무시(엔진 규칙).
     반려 후 재제출은 version을 올린 새 제안.
  4. **voting 잠금**: submit_result는 running에서만, voting 중에는 현재 제안에 대한
     vote_result만 허용, voting 중 추가 submit_result는 도메인 오류로 거부(엔진이
     강제, 상태 기계의 VOTING→VOTING 금지가 계약 차원 방어).
  5. ~~심의 중 사망자는 심의 대상에서 제외해 정족수 재계산(`with_voter_removed`)~~
     — D-018에서 번복(스냅샷 불변).
- **근거**: 사용자 설계 검토 — D-011의 "기권 제외 전원 승인"은 엄밀한 만장일치가
  아님(침묵이 승인으로 둔갑). 화백의 정체성인 만장일치를 기본값에서 엄밀하게 지키고,
  완화가 필요한 팀은 설정으로 명시적으로 선택하게 한다. 제안 버전은 반려 후 재제출과
  늦은 투표의 안전한 처리에 필수.
- **결정자**: 사용자.

## D-017 (2026-07-14) 영속화: SQLite EventStore (M2~M3)

- **결정**: 최소 SQLite `EventStore`를 도입한다 — 저장 인터페이스 + SQLite 구현으로
  제한(ORM·이벤트 소싱 프레임워크 금지). 저장 대상: sessions / agents / messages /
  proposals / votes / decisions / usage_events / session_events. 완료된 세션과 의결
  기록은 서버 재시작 후에도 조회 가능해야 하며, 실행 중 세션의 완전 복원은 M5로 유보.
- **갱신(2026-07-14 설계 동기화)**: 경계를 `store/base.py`(Store Protocol) +
  `store/sqlite.py`(구현)로 분리하고 구현 시점을 M2로 앞당김(테이블에 agents,
  session_events 추가). 재시작 시 이전 running/voting 세션 처리는 D-021 참조.
- **대안**: JSONL 파일(세션당 1파일) — M0 조사에서 검토했으나(Research §4), 화백의
  핵심 산출물이 "최종 답변 + 결정 과정·근거"라 제안/투표/의결의 구조적 조회가 필요.
- **결정자**: 사용자.

## D-018 (2026-07-14) 투표 대상자: voting 시작 시점 스냅샷 불변 (D-016 §5 번복)

- **상태**: 채택.
- **맥락**: 심의 도중 에이전트가 사망하거나 LLM 호출에 묶여 투표하지 못하는 경우,
  정족수의 분모를 어떻게 다룰지가 모호했다. D-016은 사망자를 심의 대상에서 제외해
  재계산(`with_voter_removed`)하기로 했었다.
- **결정**: voting 시작 시점의 **생존 에이전트(제출자 제외)를 투표 대상자로 확정
  (스냅샷)**하고, 심의 도중 사망해도 집합을 변경하지 않는다. 투표 불가(사망·오류)는
  voting_timeout 만료 시 기권으로 처리되어 unanimous에서는 no_quorum으로 이어진다.
  투표 대상자가 0명이면 first 모드가 아닌 한 no_quorum이며, 팀 검증 단계에서
  "first가 아닌 모드 + 2인 미만 팀"을 사전 거부한다.
- **대안**: (a) 사망 시 재계산(구 D-016) — 분모가 흔들려 "사망 타이밍에 따라 승인
  여부가 바뀌는" 비결정성 발생, (b) 사망 시 즉시 no_quorum — 재시도 여지 없이 과격.
- **선택 이유**: 판정의 결정성(같은 투표 = 같은 결과)과 감사 가능성. 화백의 만장일치
  정신에서 "심의자가 빠졌으니 남은 사람만으로 합의"는 정족수 완화의 뒷문이 된다.
- **결과와 트레이드오프**: `with_voter_removed` 계약 삭제. 사망자가 있으면
  unanimous 제안은 사실상 실패하고 세션이 no_quorum으로 끝난다 — 엄격하지만
  명시적. 완화가 필요한 팀은 participating_unanimous를 선택.

## D-019 (2026-07-14) 타이머 분리: idle_timeout / voting_timeout + approval 구조형 설정

- **상태**: 채택.
- **맥락**: idle_timeout 하나로 running 유휴 감지와 voting 투표 대기를 겸용하면,
  투표 대기 중 idle 판정이 세션을 failed(idle)로 종료하는 레이스가 생긴다.
- **결정**: `termination.idle_timeout`은 running 전용(전원 인박스 비고 호출 없음),
  `approval.voting_timeout`은 voting 전용(미완료 투표 대기)으로 분리한다. voting
  중에는 idle 감시가 세션을 종료하지 않는다. 두 타이머 모두 세션의 단일 감시
  주체가 관리한다. 팀 설정의 approval은 문자열 하나에서 구조형으로 확장:
  `approval: {mode, timeout_seconds, minimum_votes}` — 기존 문자열은 mode 축약형으로
  하위호환 유지(마이그레이션: config.py 독스트링 문서화).
- **대안**: 단일 타이머 유지(구 Plan) — 레이스 원인. voting 무제한 대기 — 교착.
- **선택 이유**: 상태별 의미가 다른 대기를 한 값으로 묶으면 튜닝 불가(빠른 idle 감지
  vs 충분한 심의 시간은 상충).
- **결과와 트레이드오프**: 계약 `ApprovalConfig` 신설, TerminationPolicy.approval
  타입 변경(하위호환은 로더가 흡수). 설정 항목이 하나 늘어남.

## D-020 (2026-07-14) 제안 상태·투표 독립 계약 + 투표 규칙

- **상태**: 채택.
- **맥락**: 의결 기록이 화백의 핵심 산출물인데, 제안의 수명주기와 개별 투표가
  Message에만 실려 있어 구조적 조회(D-017)와 안전한 재제출 처리가 어렵다.
- **결정**: `ResultProposal`에 `status: pending|approved|rejected|superseded`를
  추가하고(반려된 제안은 새 버전 제출 시 superseded), `Vote`(id/session_id/
  proposal_id/voter/decision/reason/created_at)를 독립 계약으로 둔다.
  규칙: 한 에이전트는 동일 제안에 1회만 유효 투표(**변경 금지** — MVP 추천 수용),
  제출자는 자기 제안에 투표 불가, **reject에는 사유 필수**(제출자가 보완할 근거),
  세션당 활성(pending) 제안 최대 1개.
- **대안**: 투표 변경 허용 — 판정 시점 의존 비결정성과 조기 확정 롤백 문제.
- **선택 이유**: 의결 레코드의 감사 가능성과 판정 단순성.
- **결과와 트레이드오프**: 마음을 바꾼 에이전트는 현 제안을 반려시키고 다음
  버전에서 반영해야 함 — 순회 1회 추가 비용, 대신 기록이 명확.

## D-021 (2026-07-14) 세션 종료 직렬화·우선순위 + interrupted

- **상태**: 채택 (우선순위의 실효성은 M2 경쟁 조건 테스트에서 재검증).
- **맥락**: 마지막 투표와 예산 초과, 취소와 승인 등 종료 조건이 동시에 발생할 수
  있다. 종료가 두 번 확정되면 결과/사유가 비결정적이 된다.
- **결정**: 종료 전환은 세션 단위 lock(단일 coordinator)이 직렬화하고, 최초로
  확정된 유효 종료 사유만 저장한다. 경합 시 우선순위:
  **cancelled → completed → budget/messages → agent_error → no_quorum → idle**.
  종료 후 도착한 이벤트는 상태를 변경하지 못하며 감사용 rejected event로 기록할
  수 있다. 서버 재시작 시 이전 running/voting 세션은 **failed(interrupted)**로
  처리한다 — `FailReason.INTERRUPTED` 신설 (agent_error에 뭉뚱그리지 않음,
  오류 귀책 정확성 원칙).
- **대안**: 도착 순 선착(비결정적), interrupted 없이 agent_error 재사용(귀책 왜곡).
- **선택 이유**: 사용자 의사(취소)와 이미 성립한 합의(completed)가 시스템 사정보다
  우선해야 관측 결과가 직관과 일치한다.
- **결과와 트레이드오프**: 우선순위 표가 계약 문서에 고정됨 — 실제 경쟁 조건에서
  이 순서가 필요한지 M2에서 테스트로 재검증하고, 불필요하면 단순화한다.

## D-022 (2026-07-14) 도메인 이벤트 봉투 확정, 세분 taxonomy는 D-028에서 확정

- **상태**: 봉투 채택 / taxonomy는 D-028로 해소.
- **맥락**: SSE 재연결(Last-Event-ID)과 영속화(session_events)를 위해 이벤트
  식별 체계가 지금 필요하다. 반면 세분 이벤트 타입 목록(session.*, agent.*,
  proposal.*, vote.*, limit.*)의 정확한 발행 지점은 엔진 구현 전에 확정하기 어렵다.
- **결정**: 봉투를 `event_id / session_id / type / sequence / created_at /
  payload`로 지금 확정한다(sequence = 세션 단위 단조 증가, Last-Event-ID 기준).
  내부 도메인 이벤트 세분 taxonomy는 EventContract.md에 **후보로 문서화**하고
  M2 발행 지점 구현과 함께 확정한다 — 봉투 호환은 보장.
- **대안**: 지금 전체 taxonomy를 enum으로 확정 — 발행 지점 없이 확정하면 M2에서
  재개정 확률이 높음(임의 구현 금지 원칙).
- **선택 이유**: 호환성이 필요한 부분(봉투)과 아직 모호한 부분(타입 세분)을 분리.
- **결과와 트레이드오프**: M2에서 EventType 확장 개정이 한 번 더 필요 — 대신
  봉투·영속 스키마는 재작업 없음. **위험**: 현행 6개 집계 타입을 쓰는 대시보드
  계약이 M2에서 세분 타입으로 바뀌면 EventContract 재갱신 필요.

## D-023 (2026-07-14) 메시지 순서·배달 정책

- **상태**: 채택.
- **맥락**: 단일 프로세스지만 결정적 테스트와 타임라인 복원을 위해 순서·중복·
  자기송신 정책이 필요하다.
- **결정**: (1) 메시지에 **세션 단위 단조 증가 sequence**를 계약 필드로 추가 —
  동일 timestamp의 순서는 sequence로 고정, 배치 내 정렬 기준. (2) id는 UUID 등
  전역 유일 — 버스는 동일 id 중복 배달을 무시. (3) 브로드캐스트는 원본 1건이
  수신자별 인박스에 독립 배달되며(발신자 제외), 별도 배달 id는 두지 않는다.
  (4) **자기 자신에게 보내는 메시지는 금지**(계약 검증).
- **대안**: 브로드캐스트 배달 id 분리 — 단일 프로세스 MVP에서 이득 없이 복잡.
  자기송신 허용(메모용) — 인박스 배치 병합과 idle 판정을 교란.
- **선택 이유**: MVP 권장안 수용 — 분산 보장 없이 결정성만 확보.
- **결과와 트레이드오프**: Message 생성 책임(버스가 id/created_at/sequence 부여)이
  계약에 고정됨.

## D-024 (2026-07-14) voting 중 일반 메시지 허용

- **상태**: 채택.
- **맥락**: voting 상태에서 `send_message`를 막을지 결정 필요. 상태별 허용 명령
  표를 계약에 명시하면서 함께 확정.
- **결정**: 허용한다 — `running: send_message, submit_result` /
  `voting: send_message, vote_result` / 종료 상태: 전부 거부 (`ALLOWED_COMMANDS`).
- **대안**: voting 중 메시지 금지(투표만) — 반려 사유 질의, 근거 요청 등 심의
  논의가 불가능해져 반려→재제출 루프의 품질이 떨어짐.
- **선택 이유**: 화백은 표결 전 논의가 본질이다. 남용(투표 회피 수다)은 메시지
  상한·토큰 예산·voting_timeout이 방어한다.
- **결과와 트레이드오프**: voting이 길어질 수 있으나 timeout이 상한. 논의 후
  마음이 바뀌어도 투표 변경은 불가(D-020) — 다음 버전에서 반영.

## D-025 (2026-07-14) 설계 자체 검토 반영: 초안 보존·타이머 상향·M2 분할

- **상태**: 채택.
- **맥락**: 설계 동기화 직후 자체 검토에서 4가지 개선점 발견 — (1) 기본 조합
  (unanimous + voting_timeout 30초)이 세션 맨 끝(작업이 가장 많이 투입된 시점)에서
  timeout-기권-no_quorum 실패를 양산하고 초안까지 소실, (2) EventContract가
  message payload sequence와 envelope sequence를 "동일 값"으로 잘못 서술
  (버스 카운터와 이벤트 카운터는 독립), (3) store 편입으로 M2 비대화,
  (4) no_quorum의 실패 사유가 뭉개지기 쉬움 + 이력 보호 대상의 상한 규칙 부재.
- **결정**: ① `ApprovalConfig.voting_timeout` 기본 30→**120초**.
  ② 투표까지 갔지만 확정 없이 실패한 세션은 마지막 제안을
  `Session.draft_result`/`draft_proposer`로 보존(FAILED에서만, 승인된 result와
  구분 — 대시보드는 "미승인 초안" 카드로 표시, SSE payload에는 미포함).
  ③ EventContract의 이중 sequence 서술 정정(독립 카운터 명시).
  ④ M2를 M2a(인메모리 코어+CLI smoke) / M2b(store 접목+taxonomy 확정)로 분할.
  ⑤ no_quorum 실패 시 fail_detail에 미투표/기권자 기록 의무화(M2),
  이력 절단 시 보호 대상 초과면 최신 제안 1개만 원문 보호.
- **대안**: no_quorum 시 자동 재투표/타이머 연장 — 교착 위험과 복잡도 증가로
  보류(M2 이후 리마인더 1회+연장을 선택적으로 재검토).
- **선택 이유**: "실패 경로가 제품" 원칙 — 가장 비싼 시점의 실패에서 사용자가
  빈손이 되지 않게 하는 것이 화백의 산출물 가치(결정 과정 보존)와 일치.
- **결과와 트레이드오프**: 기본 투표 대기가 길어져 최악의 경우 세션 종료가 최대
  120초 지연될 수 있음(사용자는 취소 가능). 초안 보존 필드 2개가 계약에 추가됨.
- **결정자**: Claude 제안, 사용자 승인 ("반영해").

## D-026 (2026-07-14) LLM 인증 경로: 하이브리드 (api_key 기본 + chatgpt_oauth 모드)

- **상태**: 채택.
- **맥락**: M2 착수 전 스파이크로 D-008의 전제(subscription 연동)를 검증한 결과 —
  Codex OAuth(device flow) 토큰으로 구독 과금 Responses API 호출이 **작동**하지만
  (litellm `chatgpt/` 프로바이더, openclaw 등 실사용), OpenAI가 서드파티 사용을
  공식 허용한 적 없고 Anthropic·Google은 2026년 초 동일 경로를 약관으로 차단한
  전례가 있음. 또한 구독 백엔드는 `max_output_tokens`/`metadata`를 거부해 토큰
  예산의 사전 상한이 불가(사후 집계만 가능). 상세는 Research §6.
- **결정**: openai 어댑터에 **인증 모드 2종**을 둔다 — `api_key`(기본, 공식) |
  `chatgpt_oauth`(선택, device flow). M2a는 api_key로 구현·스모크하고, M2b에서
  chatgpt_oauth 모드를 추가한다(gpt-5.6 구독 백엔드 지원 실측 포함). 인증은
  어댑터 구현 세부이므로 **LLMClient 계약은 변경 없음**. 토큰 예산은 두 모드
  모두에서 사후 집계로 강제(사전 상한은 api_key 모드에서만 추가 적용).
- **대안**: (a) OAuth 전용 — 차단 시 전면 중단, (b) api_key 전용 — 구독 연동
  전제(D-008) 폐기.
- **선택 이유**: 사용자의 비용 전제(구독 활용)를 살리면서 비공식 경로 차단
  리스크를 폴백으로 흡수. 개발·CI 경로는 공식 API로 안정화.
- **결과와 트레이드오프**: 어댑터에 인증 분기 유지 비용. OAuth 모드는 약관 변경
  시 즉시 제거될 수 있음을 README에 고지.
- **결정자**: 사용자.

## D-027 (2026-07-14) 기본 팀 확정: 대등 3인 구조 + capabilities 도구 권한

- **상태**: 채택.
- **맥락**: M1부터 미결이던 기본 팀 구성(초안: researcher/analyst/writer)에 대해
  사용자가 화백 정체성에 맞는 최종안을 제시 — "조사 대등 → 견제 대등 → 상대등"
  3인 구조 + 에이전트별 도구 권한(capabilities) 계약화.
- **결정**:
  1. 기본 팀 = `research_daedeung`(조사, send+vote) / `critic_daedeung`(견제,
     send+vote) / `sangdaedeung`(진행·종합·제출, send+submit — **자기 제안에 투표
     불가이므로 vote 권한 없음이 구조적으로 자연스러움**). 제한:
     max_messages 60 / token_budget 100k / idle 45s / voting 120s, unanimous.
  2. **`AgentCapability` 계약 신설** — 프롬프트가 아니라 런타임(SessionManager)이
     권한 밖 도구 호출을 구조화된 tool error로 거부한다. 상태별 허용
     (ALLOWED_COMMANDS)과 직교하는 축.
  3. 프롬프트 설계 원칙 채택: 첫 턴 행동 강제(상호 대기 idle 방지), 반대를 위한
     반대 금지(실질 결함만 반려 + 사유는 수정 가능한 형태), 일반 메시지와
     result_proposal 투표의 명확한 구분.
- **사용자 제안에서 조정한 것** (Claude 검토, 사용자 안대로 반영하되 3건 수정):
  (a) capabilities 기본값을 "send_message만"이 아니라 **전체 권한**으로 — 기존
  팀·테스트 하위 호환, 제한은 명시적 선택. (b) **심의자 스냅샷 자격 = 생존 ∧
  vote_result 권한** — 이 규칙이 없으면 투표 불가 에이전트가 심의자로 들어가
  unanimous가 항상 no_quorum이 됨(제안에 없던 상호작용 버그를 사전 차단). 팀
  검증에 "각 제출 가능 에이전트마다 다른 투표 가능 에이전트 1명 이상" 추가.
  (c) ToolPermissionError 신설 대신 기존 ToolError, StrEnum 대신 기존 (str, Enum)
  관례.
- **대안**: 프롬프트로만 권한 통제 — LLM이 지시를 어기면 화백 구조가 깨짐.
- **결과와 트레이드오프**: 팀 구성 표현력이 늘고 역할 강제가 구조화됨. 파생 팀
  (code-review/knowledge/career 등)은 설정 추가만으로 가능(D-004). 관찰 전용
  (빈 capabilities) 에이전트도 계약상 허용.
- **결정자**: 사용자 (구조·프롬프트), Claude (계약 통합 방식).

## D-028 (2026-07-14) 이벤트 taxonomy: 6개 집계 타입 유지 (세분 enum 미도입)

- **상태**: 채택 (D-022의 "M2에서 확정" 이행).
- **맥락**: D-022에서 도메인 이벤트 세분 taxonomy(session.*, agent.*, proposal.*,
  vote.*, limit.*)를 EventContract §8에 후보로 두고 M2 발행 지점 구현과 함께
  확정하기로 했다. M2a가 6개 집계 EventType(session_status/message/agent_state/
  usage/vote_status/result)으로 완결 동작하며 대시보드 계약(EventContract §3)도
  이 위에 서 있다.
- **결정**: **6개 집계 타입을 확정 와이어 계약으로 유지**한다. 세분 enum을 새로
  도입하지 않고, 세분 의미(예: proposal.rejected vs approved)는 기존 payload
  필드(session_status의 status/fail_reason, vote_status의 tally, agent_state의
  state/detail)로 표현한다. EventContract §8의 논리 이벤트 예시는 "payload로 식별되는
  논리적 이벤트"의 참고 매핑으로 남긴다.
- **대안**: (a) EventType을 ~20종으로 확장 — 대시보드·저장·테스트 계약을 지금
  churn시키고 얻는 것이 payload 중복뿐, (b) 이중 표기(집계 + 세분) — 복잡도만 증가.
- **선택 이유**: 이미 동작하는 최소 계약을 흔들지 않는다(과설계 회피 원칙).
  세분이 실제로 필요한 소비자(예: limit.warning 알림)가 M4 대시보드에서
  등장하면 그때 payload 필드 추가 또는 신규 타입으로 확장 — 봉투는 이미 호환.
- **결과와 트레이드오프**: limit.warning(예산 임박 경고)은 현재 별도 이벤트가
  없다 — 필요 시 usage 이벤트 소비자가 token_budget 대비 비율로 판단하거나 M4에서
  추가. EventContract §8을 "확정: 집계 유지"로 갱신.
- **결정자**: Claude 제안 (M2 실동작 근거).

## D-029 (2026-07-14) SQLite 접근 방식: 표준 sqlite3 + asyncio.to_thread

- **상태**: 채택.
- **맥락**: Research §5 미조사 항목 — aiosqlite vs 표준 sqlite3+to_thread.
- **결정**: 표준 라이브러리 sqlite3를 asyncio.to_thread로 감싼다. 신규 의존성 0,
  단일 세션·로컬 파일 규모(M2b)에 충분. 요청 경로 블로킹은 SessionManager의
  write-behind 큐(단일 라이터, 종료 시 flush)가 차단한다.
- **대안**: aiosqlite — 의존성 추가 대비 이득 없음(내부적으로 스레드 실행).

## D-030 (2026-07-14) 기본 팀 합의 모드 participating_unanimous(min 1) + max_turns 25

- **상태**: 채택 (사용자 결정).
- **맥락**: chatgpt_oauth 실 세션 3회 연속 no_quorum. DB 타임라인 실증 결과
  (WorkLog 참조): ① 심의자가 채팅으로 max_turns(15)를 voting 25초 만에 소진해
  투표가 물리적으로 불가능해짐, ② 다른 심의자는 스트림 무응답으로 세션 내내
  hang. unanimous는 "스냅샷 심의자 전원 approve"라 심의자 1명의 hang/턴
  소진만으로 no_quorum이 보장된다(기권은 어떤 정책에서도 승인이 아님 — D-020).
- **결정**:
  1. 기본 팀 approval.mode를 `unanimous` → `participating_unanimous` +
     `minimum_votes: 1`로 변경 — 유효 투표자 전원 승인 + 최소 1표. 심의자
     일부가 무력화돼도 나머지 승인으로 의결 가능하고, 반대표는 여전히 반려,
     전원 기권은 minimum_votes에 걸려 no_quorum.
  2. 3인 전원 max_turns 15/18 → 25 — 구독 모드는 토큰당 비용이 없고(D-026),
     토론+투표를 한 예산 안에서 소화할 여유 확보. 폭주는 max_messages(60)와
     token_budget(100k)이 계속 막는다.
- **대안**: unanimous 유지(엄밀한 화백이지만 실패율 과다), majority(2인
  심의자 기준 1명 hang 시 과반 불성립이라 이점 없음).
- **결정자**: 사용자.

## D-012 (2026-07-14) 대시보드 접근: localhost 전용

- **결정**: 서버는 `127.0.0.1`에만 바인딩하고 인증은 두지 않는다.
- **대안**: LAN 허용 + 토큰 인증, localhost 기본 + LAN 옵션.
- **근거**: 개인 도구로서 보안 설계 최소화, M3 단순화. 페르소나 P-02(비개발자 동료,
  LAN 접근 암시)는 **확장 시나리오로 이연** — LAN 공유가 필요해지면 토큰 인증과 함께
  이 결정을 갱신한다.
- **결정자**: 사용자 (Claude 추천 수용).

## D-013 (2026-07-14) 세션 동시 실행: 1개만

- **결정**: running/voting 세션이 있으면 새 태스크 제출을 거부한다(안내 메시지 표시).
- **대안**: 전역 상한 N개, 제한 없음.
- **근거**: 비용 상한이 명확해지고(핵심 설계 과제 2 — 비용 통제), 대시보드 관찰도
  한 세션에 집중되는 개인 도구 용법에 부합. 상한 N 확장이 필요해지면 이 결정을 갱신.
- **결정자**: 사용자 (Claude 추천 수용).

## D-014 (2026-07-14) 패키징: setuptools + src 레이아웃, Python 3.11 하한

- **결정**: `pyproject.toml`(setuptools) + `src/hwabaek/` 레이아웃, 개발 설치는
  `pip install -r requirements.txt`(내용은 `-e .`). 런타임 의존성은 당분간 pyyaml만.
  venv는 네이티브 Windows Python 3.11(`py -m venv .venv`)로 생성 — 하한 버전으로 검증.
- **근거**: src 레이아웃은 테스트가 설치된 패키지를 임포트하게 강제해 경로 사고를 방지.
  git-bash의 python은 MSYS2 빌드라 venv 레이아웃이 달라짐(`bin/`) — 프로젝트 규칙
  (`.venv\Scripts\python.exe`)과 일치하도록 네이티브 인터프리터 사용을 명시.
- **주의**: requirements.txt 등 pip이 파싱하는 파일은 **ASCII만** — 이 환경(cp949)에서
  pip이 UTF-8 한글 주석을 디코드하지 못한다 (실제 발생, WorkLog 참조).
- **결정자**: Claude (관례 수준의 선택, 이의 시 갱신).
